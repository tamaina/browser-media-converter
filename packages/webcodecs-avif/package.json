{
  "name": "@browser-avif-lab/webcodecs-avif",
  "version": "0.0.0",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "esbuild src/index.ts --bundle --format=esm --platform=browser --target=es2022 --outfile=dist/index.js && tsc -p tsconfig.json --emitDeclarationOnly",
    "pretypecheck": "pnpm --filter @browser-avif-lab/media-container build",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "pnpm build"
  },
  "dependencies": {
    "@browser-avif-lab/binary": "workspace:*",
    "@browser-avif-lab/media-container": "workspace:*"
  }
}
